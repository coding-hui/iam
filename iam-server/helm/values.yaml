# Default values for helm.
# This is a YAML-formatted file.
# Declare variables to be passed into your templates.

replicaCount: 1

image:
  repository: devops-wecoding-docker.pkg.coding.net/wecoding/docker-repo/iam
  pullPolicy: Always
  # Overrides the image tag whose default is the chart appVersion.
  tag: "v0.2"

imagePullSecrets:
  - name: "devops-wecoding"
nameOverride: ""
fullnameOverride: "iam-server"

serviceAccount:
  # Specifies whether a service account should be created
  create: true
  # Annotations to add to the service account
  annotations: {}
  # The name of the service account to use.
  # If not set and create is true, a name is generated using the fullname template
  name: ""

podAnnotations: {}

podSecurityContext: {}
  # fsGroup: 2000

securityContext: {}
  # capabilities:
  #   drop:
  #   - ALL
  # readOnlyRootFilesystem: true
  # runAsNonRoot: true
  # runAsUser: 1000

service:
  type: NodePort
  port: 80

ingress:
  enabled: false
  className: ""
  annotations: {}
    # kubernetes.io/ingress.class: nginx
    # kubernetes.io/tls-acme: "true"
  hosts:
    - host: iam-back.wecoding.top
      paths:
        - path: /
          pathType: ImplementationSpecific
  tls: []
  #  - secretName: chart-example-tls
  #    hosts:
  #      - chart-example.local

resources:
  # We usually recommend not to specify default resources and to leave this as a conscious
  # choice for the user. This also increases chances charts run on environments with little
  # resources, such as Minikube. If you do want to specify resources, uncomment the following
  # lines, adjust them as necessary, and remove the curly braces after 'resources:'.
  limits:
    cpu: 1000m
    memory: 1024Mi
  requests:
    cpu: 500m
    memory: 1024Mi

autoscaling:
  enabled: false
  minReplicas: 1
  maxReplicas: 100
  targetCPUUtilizationPercentage: 80
  # targetMemoryUtilizationPercentage: 80

nodeSelector: {}

tolerations: []

affinity: {}

kubernetesClusterDomain: cluster.local

config:
  redis:
    host: redis-svc.wecoding-depend
    database: 0
    password:
  mysql:
    driverClassName: com.mysql.cj.jdbc.Driver
    host: 10.0.4.15
    port: 3306
    db: wecoding_iam
    auth:
      username: root
      password: WeCoding319!@.
    nacos:
      discovery:
        group: wecoding-system
        serverAddr: http://nacos-cs:8848
      config:
        serverAddr: http://nacos-cs:8848