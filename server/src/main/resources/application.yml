server:
  port: 80
  servlet:
    context-path: /

spring:
  application:
    name: wecoding-iam
  servlet:
    multipart:
      max-request-size: 100MB
      max-file-size: 100MB
  cache:
    type: redis
  main:
    allow-bean-definition-overriding: true
  mvc:
    pathmatch:
      matching-strategy: ant_path_matcher
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://sh-cynosdbmysql-grp-hrw08c4a.sql.tencentcdb.com:25863/wecoding?autoReconnect=true&useUnicode=true&characterEncoding=utf8&zeroDateTimeBehavior=CONVERT_TO_NULL&useSSL=false&serverTimezone=CTT
    username: root
    password: WeCoding319!@.
  redis:
    host: localhost
    port: 6379
    database: 0
    password:
    timeout: 10s

# 日志配置
logging:
  level:
    top.wecoding: debug
    org.springframework: warn
    com.baomidou.dynamic: warn
