apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "wecoding-iam.fullname" . }}-test-connection"
  labels:
    {{- include "wecoding-iam.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "wecoding-iam.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
